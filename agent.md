# View / API 설계 방침

## 목표

* View는 HTTP 입력, 화면 조립, 리다이렉트만 담당한다.
* API는 데이터 조회·수정, 권한 검사, 입력 검증, 트랜잭션 경계를 담당한다.
* API는 View 없이 단독으로 호출해도 정상 동작해야 하며, 호출자가 처리를 끝낼 수 있는 데이터 또는 명확한 오류를 반환한다.
* 새 라우트는 쿼리 파라미터 대신 경로 파라미터를 사용한다. 기존 URL 호환성은 라우트 변경이 명시된 경우가 아니면 유지한다.
* 기존 동작을 유지할 수 있는 가장 작은 구현을 우선한다. 실제 중복 제거 또는 View/API 책임 분리에 필요하지 않다면 추상화를 추가하지 않는다.
* 작업 목적과 직접 관련 없는 파일 정리, 이름 변경, 스타일 통일, 추상화 변경, 대규모 리팩터링은 하지 않는다.
* 기능을 추가하기 위해 시스템 전체를 복잡하게 만들지 않는다. 기존 구조와 호환성을 최대한 유지하면서 필요한 기능을 가장 작은 코드와 가장 적은 의존성으로 구현한다.

## 코드 스타일 원칙

* 변수명과 함수명에는 `snake_case`를 사용한다.
* 함수명은 반드시 첫 글자를 대문자로 시작한다.
* API 함수명은 반드시 `Api_`로 시작한다. 예: `Api_document_get`, `Api_user_permission_check`.
* View 함수명은 반드시 `View_`로 시작한다. 예: `View_document`, `View_history`.
* API 파일명은 반드시 `api_`로 시작한다. 예: `api_document.go`, `api_user.go`.
* View 파일명은 반드시 `view_`로 시작한다. 예: `view_document.go`, `view_history.go`.
* API 기능을 `view_*.go` 파일에 구현하거나 View 기능을 `api_*.go` 파일에 구현하지 않는다.
* API 또는 View 역할을 하는 새 함수와 파일에 다른 접두사를 임의로 사용하지 않는다.
* 일반 내부 헬퍼처럼 API 또는 View에 해당하지 않는 함수는 해당 역할의 접두사를 강제로 붙이지 않되, 함수명의 첫 글자는 대문자로 시작하고 `snake_case`를 사용한다.
* 새 함수에 `camelCase` 또는 `PascalCase` 형태의 이름을 사용하지 않는다.
* 변수명은 소문자 `snake_case`를 기본으로 한다. 예: `document_name`, `user_id`, `is_admin`.
* 상수와 기존 공개 인터페이스는 기존 코드와의 호환성을 우선하며, 스타일 통일만을 목적으로 이름을 변경하지 않는다.
* 기존 함수나 변수의 이름은 현재 작업에서 변경할 필요가 없다면 스타일 통일만을 위해 일괄 변경하지 않는다.
* 새 코드에서는 주변 코드가 다른 스타일을 사용하더라도 본 규칙을 우선한다.
* 이름은 가능한 한 역할을 직접 드러내도록 작성하며, 의미를 알기 어려운 지나치게 짧은 이름은 반복문 인덱스 등 명확한 경우를 제외하고 사용하지 않는다.

## View 및 UX/UI 원칙

* `view_*.go` 파일에서 SQL을 직접 작성하지 않는다.
* View 라우트와 View의 주요 처리 함수는 `View_*` 함수에 구현한다.
* View에 필요한 도메인 데이터는 기존 `Api_*` 함수로 먼저 가져온다.
* View는 언어, 템플릿, URL, HTML escape, UI 같은 표시용 헬퍼를 사용할 수 있다.
* 아직 API·도메인 함수로 옮기지 않은 기존 표시용 헬퍼가 필요한 경우에만 View에서 DB 연결을 유지한다.
* View가 DB 연결을 가지고 있는 것 자체는 기존 코드 호환을 위해 허용할 수 있지만, 새 직접 SQL을 추가하는 근거가 되지는 않는다.
* 권한 검사와 쓰기 입력 검증을 View에만 두지 않는다. View 검사는 UI 편의를 위한 보조일 수 있지만, API가 같은 규칙을 반드시 강제해야 한다.
* View는 API가 반환한 상태에 따라 화면 출력, 오류 표시, 인증 유도, 리다이렉트 등 HTTP 및 UI 처리를 수행한다.
* 사용자에게 불필요한 불편을 주지 않는 범위에서 UI는 가능한 한 단순하게 구현한다.
* 기능 구현을 위해 필요하지 않은 버튼, 메뉴, 단계, 모달, 설정 항목, 별도 화면 등 UI 요소와 상호작용을 임의로 추가하지 않는다.
* 기존의 간단한 동작으로 충분하다면 더 복잡한 UI 구조로 바꾸지 않는다.
* 서로 연관된 기능과 정보는 가능한 한 같은 영역 또는 인접한 영역에 배치한다.
* 하나의 작업 흐름에 속하는 입력, 결과, 보조 기능을 불필요하게 서로 떨어진 위치에 배치하지 않는다.
* 사용자가 특정 기능을 수행하기 위해 화면의 여러 영역을 오가거나 관련 기능을 찾아다녀야 하는 구조를 피한다.
* 기능의 위치는 단순한 시각적 균형보다 사용 흐름과 기능적 연관성을 우선한다.
* 화면 구성은 기능 단위를 기준으로 위에서 아래로 읽었을 때 자연스럽게 이해될 수 있도록 한다.
* 관련 기능을 가까이 배치하더라도 한 행에 여러 조작 요소를 억지로 압축하지 않는다.
* 화면상의 한 행에는 하나의 기능 또는 하나의 주요 조작 요소만 배치한다.
* `button`, `input`, `select`, `textarea` 등 서로 다른 조작 요소를 같은 행에 나란히 배치하지 않는다.
* 입력 요소가 있는 행에는 해당 입력 요소를 중심으로 구성하고, 실행 버튼이 필요한 경우 가능한 한 별도의 행에 배치한다.
* 여러 기능을 한 행에 압축하여 화면을 작게 만드는 것보다 각 기능의 역할이 명확하게 구분되는 단순한 배치를 우선한다.
* 단순한 공간 확보를 목적으로 `<br>`을 반복해서 사용하지 않는다.
* 렌더러가 생성하는 사용자 문서 콘텐츠를 제외한 openNAMU 내부 UI에서는 기능이나 영역을 구분할 때 `<br>`보다 `<hr class="main_hr">` 사용을 우선한다.
* `<hr class="main_hr">`은 내부 화면의 기능 또는 영역 구분 용도로 사용하며, 단순히 문장 안에서 줄을 바꾸기 위한 용도로 사용하지 않는다.
* 기존 내부 화면에서 `<br>`을 새로 추가해야 할 경우 `<hr class="main_hr">`으로 목적을 달성할 수 있는지 먼저 확인한다.
* 렌더러 출력은 문서 문법과 사용자 콘텐츠의 표현을 우선하므로 위 `<br>` 및 `<hr class="main_hr">` 원칙의 적용 대상에서 제외한다.
* JavaScript 없이 구현할 수 있는 기능은 가능한 한 서버 렌더링, HTML, CSS만으로 구현한다.
* JavaScript는 사용자 편의나 기능상 명확한 이점이 있을 때만 추가한다. 단순한 화면 전환이나 서버에서 처리할 수 있는 동작을 위해 불필요하게 도입하지 않는다.
* JavaScript가 필요한 경우에도 해당 기능에 필요한 최소 범위로 제한하며, 단순한 기능을 위해 전역 상태나 대규모 프론트엔드 구조를 만들지 않는다.
* 새 사용자 표시 문자열은 기존 언어 시스템을 사용하며 View나 API에 특정 언어의 사용자 표시 문자열을 직접 하드코딩하지 않는다.
* 기존 번역 키를 재사용할 수 있다면 같은 의미의 새 번역 키를 만들지 않는다.

## API 원칙

* API의 외부 호출 진입점은 `Api_*` 함수로 구현한다.
* `Api_*` 함수는 보호된 데이터를 반환하거나 상태를 바꾸기 전에 스스로 권한을 검사한다.
* 현재 View가 이미 검사하더라도 API에서 모든 외부 입력값과 식별자를 검증한다.
* API 동작에 필요한 입력 정규화(trim, 형식 변환, 기본값 처리 등)는 View에 의존하지 않고 API 또는 공용 헬퍼에서 수행한다.
* 반환 map의 키와 Go 타입은 모든 호출자에서 일관되게 유지한다. 오류 또는 선택 값에 대해 View가 검증 없는 타입 단언을 하지 않도록 한다.
* API는 `ok`, `require auth`, `not exist`, 검증 오류 등 호출자가 후속 처리를 결정하는 데 필요한 상태를 명확히 구분해서 반환한다.
* 같은 종류의 오류는 기존 API의 오류 코드와 반환 형식을 재사용하며, 동일한 의미를 가진 새 오류 표현을 임의로 추가하지 않는다.
* 잘못된 입력이나 처리 실패를 임의의 기본값으로 조용히 대체하여 성공한 것처럼 처리하지 않는다.
* 오류를 복구할 명확한 기존 동작이 없다면 호출자가 판단할 수 있도록 실패를 명시적으로 전달한다.
* 읽기 API는 상태를 변경하지 않는다. 쓰기 API는 필요한 쓰기 작업 전체를 책임진다.
* 함께 성공하거나 실패해야 하는 DB 쓰기는 API가 소유하는 하나의 트랜잭션으로 묶는다.
* 트랜잭션 내부에서 새 DB 연결을 여는 API를 호출하지 않는다. 트랜잭션 안에서 데이터가 필요하면 현재 `tool.DB_runner`를 받는 작은 내부 헬퍼를 사용한다.
* 트랜잭션 내부에서 롤백할 수 없는 외부 부작용을 임의로 수행하지 않는다. 파일, 캐시, 외부 시스템 등 DB 트랜잭션으로 되돌릴 수 없는 작업이 필요한 경우 DB 작업과의 실행 순서와 실패 처리를 명시적으로 설계한다.
* 읽을 수 있는 문서 설정 목록과 쓸 수 있는 문서 설정 목록은 분리한다. `last_edit` 같은 내부 값은 읽기 허용 때문에 쓰기 허용이 되면 안 된다.

## 구현 원칙

* 새 함수, 헬퍼, 상수, 설정, 오류 코드 등을 추가하기 전에 저장소에 같은 목적의 기존 구현이 있는지 검색한다.
* 비슷한 구현이 이미 있다면 새 구현을 병렬로 만들기보다 기존 구현을 재사용하거나 필요한 최소 범위에서 확장한다.
* 동일한 규칙이나 상수 값을 여러 위치에 각각 정의하지 않는다.
* 새 외부 라이브러리를 추가하기 전에 Go 표준 라이브러리와 기존 의존성으로 구현할 수 있는지 먼저 확인한다.
* 작은 기능 하나를 위해 큰 라이브러리나 프레임워크를 추가하지 않는다.
* 새 런타임, 별도 서비스, Node.js/npm 등 추가 실행 환경을 요구하는 구조는 명시적인 필요가 없는 한 도입하지 않는다.
* 요청 시 바로 처리할 수 있는 작업을 위해 별도 작업 큐, 백그라운드 워커, 이벤트 시스템 등을 도입하지 않는다.
* 캐시, 비동기 처리, 백그라운드 작업은 실제 성능 또는 기능상 필요가 확인된 경우에만 추가한다.
* 가독성과 단순성을 희생하는 성능 최적화는 실제 병목이 확인된 경우에만 수행한다.
* 추측에 기반해 캐시, 병렬 처리, 복잡한 자료구조를 먼저 도입하지 않는다.
* 성능 문제를 이유로 구조를 복잡하게 변경할 경우 가능한 한 측정 결과나 재현 가능한 병목을 근거로 한다.
* 애플리케이션 코드는 특별한 이유가 없는 한 특정 운영체제에 의존하지 않는다.
* 파일 경로 처리는 문자열 결합보다 `filepath` 등 Go의 크로스플랫폼 방식을 우선한다.
* 애플리케이션 기능을 위해 특정 셸, 외부 명령, 운영체제 전용 기능에 의존하는 구현은 가능한 한 피한다.

## 보안 원칙

* 리팩터링이나 기능 단순화를 이유로 기존 인증, 권한, 세션, 입력 검증 등 보안 검사를 제거하거나 약화하지 않는다.
* 사용자 입력과 사용자 작성 콘텐츠는 신뢰하지 않으며, SQL, HTML, URL 등 실제로 사용되는 문맥에 맞는 기존 안전 처리 방식을 사용한다.
* 문자열 결합으로 SQL, HTML, URL 등을 직접 만드는 것보다 기존의 안전한 헬퍼나 파라미터 처리 방식을 우선한다.
* 보안 검사를 중복 제거할 경우 검사가 사라지는 것이 아니라 책임을 가지는 계층으로 이동했는지 확인한다.
* 권한 검사는 UI 요소의 노출 여부와 분리해서 생각한다. 버튼이나 링크가 숨겨져 있더라도 서버 측 권한 검사는 반드시 유지한다.
* 입력 검증과 escape는 서로 다른 책임으로 취급한다. escape가 입력 검증을 대신하거나 입력 검증이 출력 escape를 대신한다고 가정하지 않는다.

## 데이터 보존 원칙

* 기존 데이터베이스와 저장된 문서는 가능한 한 그대로 사용할 수 있어야 한다.
* 스키마 변경은 기존 데이터를 손실시키지 않는 방향으로 수행한다.
* 명시적인 요구 없이 테이블, 컬럼, 기존 데이터를 삭제하거나 기존 값의 의미를 변경하지 않는다.
* 마이그레이션이 필요한 경우 기존 설치에서 업데이트해도 정상 동작하도록 한다.
* 화면 표시나 내부 구현 변경을 위해 저장된 문서 원문 자체를 불필요하게 변환하거나 다시 저장하지 않는다.
* 데이터 형식 변경이 필요한 경우 기존 형식의 읽기 호환성을 가능한 한 유지하고, 변환이 필요한 시점과 범위를 명확히 한다.
* 데이터 삭제나 대량 변경이 포함되는 작업은 해당 변경이 실제 요구 사항인지 다시 확인하고, 코드 구조 정리만을 이유로 수행하지 않는다.

## 문서명 / 인코딩 원칙

* 문서명, URL, HTML escape, 인코딩·디코딩 처리는 기존 공용 헬퍼와 기존 규칙을 우선 사용한다.
* 같은 목적의 인코딩, 정규화, escape 로직을 각 View나 API에서 별도로 구현하지 않는다.
* 문서명의 공백, 특수문자, Unicode 등을 임의로 정규화하여 기존 문서와 다른 이름으로 취급되게 하지 않는다.
* escape와 unescape는 필요한 입출력 경계에서 수행하며, 중복 escape나 불필요한 원문 변환을 하지 않는다.
* URL 표현과 실제 문서명을 같은 값이라고 가정하지 않는다. URL decode 이후의 실제 문서 식별 규칙은 기존 동작을 따른다.
* 대소문자, 공백, Unicode 정규화 방식 등 문서 식별에 영향을 주는 동작을 명시적인 요구 없이 변경하지 않는다.

## 호환성 원칙

* DB 관련 변경은 SQLite와 MySQL에서 동일한 기능과 의미로 동작해야 한다.
* 특정 DB에만 존재하는 SQL 문법이나 동작에 의존하지 않는다. 불가피한 경우 DB별 처리를 명확히 분리한다.
* DB 쿼리나 스키마 관련 동작을 변경한 경우 가능한 범위에서 SQLite와 MySQL 양쪽을 모두 고려해 검토한다.
* 기존 스킨이나 사용자 정의 코드가 사용할 가능성이 있는 HTML 구조, class, id, 템플릿 값은 기능상 필요 없이 변경하지 않는다.
* 내부 코드를 정리하거나 HTML을 더 깔끔하게 만들기 위한 목적만으로 기존 스킨 호환성을 희생하지 않는다.
* 스킨이나 기존 사용자 정의 코드와의 호환성을 깨는 변경은 명시적인 요구가 있거나 변경이 불가피할 때만 수행한다.
* 기존 URL, 설정, API 반환 형식, HTML 인터페이스 등 외부에서 의존할 수 있는 동작은 명시적인 변경 요구가 없다면 유지한다.

## 작업 및 검증 원칙

* 작업 중 생성하는 임시 파일, 디버깅 출력, 테스트 산출물, 브라우저 스크린샷 등 저장소에 남길 필요가 없는 파일은 프로젝트 루트의 `./tmp`에 저장한다.
* 임시 파일을 소스 코드 디렉터리나 프로젝트 루트에 흩어 놓지 않는다.
* `./tmp`의 파일은 구현에 필요한 정식 리소스로 사용하지 않으며, 작업 완료 후 없어져도 동작에 영향을 주지 않아야 한다.
* 실행 가능한 환경이 있다면 빌드와 자동 테스트만으로 검증을 끝내지 않고, 가능한 한 실제 서버를 실행하여 브라우저에서 변경된 기능을 직접 확인한다.
* 브라우저 검증에서는 최소한 변경된 주요 동작, 입력 처리, 권한에 따른 동작, 리다이렉트, 오류 표시가 기존 의도대로 작동하는지 확인한다.
* UI 변경이 없는 작업이라도 HTTP 라우트나 View 동작에 영향을 주는 경우 가능하면 실제 요청을 통해 동작을 확인한다.
* 테스트 과정에서 필요한 스크린샷이나 기타 증거 자료 역시 `./tmp`에 저장한다.
* 재현 가능한 버그를 수정할 때는 가능한 한 해당 버그가 다시 발생하지 않음을 확인하는 최소한의 회귀 테스트를 추가한다.
* 테스트 하나를 추가하기 위해 실제 구현보다 더 복잡한 테스트 구조나 새로운 대형 테스트 의존성을 도입하지 않는다.
* 자동화된 회귀 테스트 추가가 현실적으로 어렵다면 최소한 실제 동작을 재현하여 수정 전후 결과를 확인한다.

## 전환 원칙

* 한 번에 하나의 View 조회 경로만 옮기고, 명시된 요구가 없다면 URL, 권한 동작, 응답 동작, HTML 출력은 유지한다.
* 새 API를 만들기 전에 기존 API와 `route/tool` 읽기 헬퍼를 먼저 재사용한다.
* 필요한 데이터가 기존 API에 없을 때만 작은 전용 API를 추가한다. 단일 조회를 위해 manager, repository, service, graph 같은 계층을 추가하지 않는다.
* 직접 SQL을 제거할 때는 필요한 데이터만 반환하는 가장 단순한 API 또는 내부 헬퍼를 만든다.
* 기존 구조가 다소 불완전하더라도 현재 작업의 목적과 직접 관련이 없다면 함께 정리하지 않는다.
* 작업 범위를 넘어서는 파일 이동, 함수명 변경, 코드 스타일 통일, 공통화, 구조 재설계는 별도 요구가 없는 한 수행하지 않는다.
* 전환 뒤에는 해당 View의 남은 직접 SQL과 모든 API 호출자를 검색해 확인한다.
* 빌드와 테스트를 수행한다. `map[string]any` 경로는 컴파일만으로 타입 불일치를 잡을 수 없으므로, 방어적 타입 검사 또는 집중 테스트를 추가한다.
* 실제 실행이 가능한 변경은 가능한 한 서버와 브라우저를 사용해 최종 동작까지 확인한다.

## 검토 목록

* 새 API 파일명이 `api_*.go`이고 주요 함수가 `Api_*`인가?
* 새 View 파일명이 `view_*.go`이고 주요 함수가 `View_*`인가?
* API 코드와 View 코드가 서로의 파일에 섞여 있지 않은가?
* 새 함수명이 대문자로 시작하는 `snake_case`인가?
* 새 변수명이 소문자 `snake_case`인가?
* 스타일 통일만을 위해 기존 공개 이름이나 관련 없는 코드를 변경하지 않았는가?
* View에 직접 SQL이 남아 있는가?
* View가 기존 DB 연결을 유지한다는 이유로 새 직접 SQL을 추가하지 않았는가?
* API가 View 없이 단독으로 올바르게 동작하는가?
* API가 권한 검사와 입력 검증을 직접 하는가?
* API 동작에 필요한 입력 정규화가 View에만 존재하지 않는가?
* 같은 의미의 오류 코드나 반환 형식을 새로 중복해서 만들지 않았는가?
* 오류가 조용히 무시되거나 정상 결과처럼 바뀌고 있지 않은가?
* API 반환 타입이 모든 호출자와 일치하는가?
* 트랜잭션의 읽기·쓰기가 같은 연결과 트랜잭션을 사용하는가?
* 트랜잭션 내부에 롤백할 수 없는 외부 부작용이 섞여 있지 않은가?
* 기존 인증, 권한, 세션, 입력 검증이 약화되지 않았는가?
* 보안 검사를 다른 계층으로 옮겼다면 실제로 동일하거나 더 강하게 유지되고 있는가?
* 사용자 입력이 사용되는 문맥에 맞게 처리되고 있는가?
* SQLite와 MySQL에서 의미가 달라질 수 있는 변경이 없는가?
* 스키마나 데이터 변경으로 기존 설치의 데이터가 손실되거나 의미가 바뀌지 않는가?
* 저장된 문서 원문을 불필요하게 변환하거나 다시 저장하고 있지 않은가?
* 기존 문서명이 공백, 특수문자, Unicode 처리 변경으로 다르게 해석되지 않는가?
* URL, HTML, 문서명 처리에 기존 헬퍼 대신 중복 로직을 새로 만들지 않았는가?
* 기존 스킨이 사용하는 HTML 구조, class, id, 템플릿 값을 불필요하게 변경하지 않았는가?
* 새 함수, 헬퍼, 상수, 설정, 오류 코드를 만들기 전에 기존 구현을 검색했는가?
* 비슷한 구현이 이미 있는데 새 구현을 병렬로 추가하지 않았는가?
* 새 외부 의존성이나 실행 환경을 꼭 추가해야 하는가?
* JavaScript 없이 더 단순하게 구현할 수 없는가?
* 새 사용자 표시 문자열이 기존 언어 시스템을 사용하고 있는가?
* 특정 운영체제나 셸에 불필요하게 의존하지 않는가?
* 실제로 필요하지 않은 캐시, 비동기 처리, 작업 큐, 백그라운드 워커를 추가하지 않았는가?
* 측정되지 않은 성능 문제를 해결하기 위해 코드를 복잡하게 만들지 않았는가?
* 서로 연관된 기능과 정보가 불필요하게 멀리 떨어져 있지 않은가?
* 하나의 작업 흐름을 위해 사용자가 화면의 여러 영역을 오가도록 만들지 않았는가?
* 화면이 기능 단위로 위에서 아래로 자연스럽게 읽히는가?
* 관련 기능을 가깝게 배치한다는 이유로 여러 조작 요소를 한 행에 억지로 몰아넣지 않았는가?
* 화면상의 한 행에 서로 다른 주요 기능이나 조작 요소를 둘 이상 배치하지 않았는가?
* `button`, `input`, `select`, `textarea` 등을 불필요하게 같은 행에 배치하지 않았는가?
* 렌더러 외 내부 UI에서 단순한 화면 구분을 위해 `<br>`을 사용하고 있지 않은가?
* 내부 UI의 영역 구분에 가능한 경우 `<hr class="main_hr">`을 사용했는가?
* 재현 가능한 버그 수정에 회귀 테스트 또는 최소한 재현 기반 검증이 포함되었는가?
* 라우트와 UI의 기존 동작이 유지되는가?
* UI가 기능에 필요한 수준보다 불필요하게 복잡해지지 않았는가?
* 새 코드가 기존 코드보다 복잡한 구조나 불필요한 추상화를 만들지 않는가?
* 작업 범위와 직접 관련 없는 리팩터링이나 정리가 포함되지 않았는가?
* 임시 파일과 스크린샷이 `./tmp` 외부에 남아 있지 않은가?
* 실행 가능한 경우 실제 서버와 브라우저를 이용해 변경된 동작을 확인했는가?
