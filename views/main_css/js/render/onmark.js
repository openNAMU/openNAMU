"use strict";

class opennamu_render_onmark {
    constructor(
        render_part_id,
        render_part_id_after,
        render_part_id_add,
        doc_name
    ) {
        this.doc_data = doc_data;
        this.doc_name = doc_name;
    }
    
    text_part() {
        return doc_data;
    }
}